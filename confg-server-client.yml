management:
  endpoints:
    web:
      base-path: /dashboard-default
      exposure:
        include: health
  endpoint:
    health:
      show-details: always
  health:
    defaults:
      enabled: true