management:
  endpoints:
    web:
      base-path: /dashboard-prod
      exposure:
        include: health
  endpoint:
    health:
      show-details: always
  health:
    defaults:
      enabled: true