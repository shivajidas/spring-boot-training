management:
  endpoints:
    web:
      base-path: /dashboard
      exposure:
        include: health
  endpoint:
    health:
      show-details: always
    metrics:
      enabled: false
  health:
    defaults:
      enabled: true